// not-found.component.ts (dummy)
import { Component } from '@angular/core';

@Component({
  selector: 'app-not-found',
  template: '' // vacío; nunca se muestra si el guard redirige
})
export class NotFoundComponent {}
